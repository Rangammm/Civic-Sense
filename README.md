# Nova — Responsive Website (HTML/CSS/JS)

## Run it

Option A (quickest): open `index.html` in your browser.

Option B (recommended local server):

```powershell
cd "C:\Users\ranga\Desktop\AI WEB"
python -m http.server 5173
```

Then visit `http://localhost:5173`.

## Files

- `index.html`: page structure/sections
- `styles.css`: responsive styling + breakpoints
- `script.js`: mobile menu + small UI behavior

